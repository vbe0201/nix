{...}: {
  mine = {
    alacritty.enable = true;

    apps = {
      dev.enable = true;
      productivity.enable = true;
      social.enable = true;
      utils.enable = true;
    };

    firefox = {
      enable = true;
      defaultBrowser = true;
    };

    fonts.enable = true;

    git.enable = true;

    gpg.enable = true;

    tmux.enable = true;

    vscode.enable = true;

    xdg.enable = true;

    zed.enable = true;

    zsh.enable = true;
  };
}
